# Icon

O componente `Icon` é utilizado para exibir ícones em sua aplicação. Ele suporta ícones de diferentes bibliotecas, incluindo FontAwesome e Material Icons, bem como SVGs personalizados.

## Importação

```tsx
import { Icon } from '@platformbuilders/fluid-react-native';
```

## Uso Básico

```tsx
<Icon name="user" />
```

## Diferentes Tipos de Ícones

O componente suporta diferentes bibliotecas de ícones:

```tsx
// FontAwesome (padrão)
<Icon name="home" type="font-awesome" />

// Material Icons
<Icon name="home" type="material" />

// FontAwesome Brands
<Icon name="github" type="fa-brands" />

// FontAwesome Light
<Icon name="star" type="fa-light" />

// FontAwesome Regular
<Icon name="heart" type="fa-regular" />

// FontAwesome Solid
<Icon name="user" type="fa-solid" />
```

## Personalização

Os ícones podem ser personalizados com diferentes tamanhos e cores:

```tsx
<Icon name="star" size={30} color="#FFD700" />
```

## Ícones Clicáveis

Por padrão, os ícones são clicáveis. Você pode desabilitar isso ou adicionar uma função de clique:

```tsx
// Desabilitar clique
<Icon name="info" touchable={false} />

// Adicionar ação de clique
<Icon
  name="trash"
  color="red"
  onPress={() => handleDelete()}
/>
```

## Usando Emojis como Ícones

O componente também permite usar emojis como ícones:

```tsx
<Icon name="👍" isEmoji size={24} />
<Icon name="🔥" isEmoji size={30} color="red" />
<Icon name="🚀" isEmoji size={32} />
```

## Com Fundo e Borda

```tsx
<Icon
  name="plus"
  backgroundColor="#E1F5FE"
  borderColor="#0288D1"
  size={24}
  color="#0288D1"
  style={{ padding: 8, borderRadius: 20, borderWidth: 1 }}
/>
```

## SVGs Personalizados

O componente suporta o uso de SVGs personalizados definidos em `assets/svg`:

```tsx
// Supondo que você tenha um SVG chamado IconLogo em assets/svg
<Icon name="Logo" />

// Você também pode personalizar o tamanho do SVG
<Icon name="Logo" width={100} height={50} />
```

## Integração com Conjuntos de Ícones Personalizados

Você pode fornecer conjuntos de ícones personalizados:

```tsx
const customIconSets = {
  IconCustom: MyCustomIconComponent,
};

<Icon name="Custom" iconSets={customIconSets} />;
```

## Propriedades

| Propriedade          | Tipo                   | Padrão         | Descrição                                                                                                        |
| -------------------- | ---------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------- |
| `name`               | string                 | ''             | Nome do ícone a ser exibido                                                                                      |
| `type`               | string                 | 'font-awesome' | Biblioteca de ícones a ser usada ('font-awesome', 'material', 'fa-brands', 'fa-light', 'fa-regular', 'fa-solid') |
| `size`               | number                 | 20             | Tamanho do ícone em pixels                                                                                       |
| `color`              | string                 | -              | Cor do ícone (padrão usa a cor do tema)                                                                          |
| `touchable`          | boolean                | true           | Se o ícone é clicável                                                                                            |
| `onPress`            | function               | () => {}       | Função chamada quando o ícone é pressionado                                                                      |
| `backgroundColor`    | string                 | 'transparent'  | Cor de fundo do ícone                                                                                            |
| `borderColor`        | string                 | 'transparent'  | Cor da borda em torno do ícone                                                                                   |
| `isEmoji`            | boolean                | false          | Se o ícone é um emoji                                                                                            |
| `iconSets`           | object                 | -              | Conjuntos personalizados de ícones                                                                               |
| `width`              | number                 | -              | Largura personalizada (para SVGs)                                                                                |
| `height`             | number                 | -              | Altura personalizada (para SVGs)                                                                                 |
| `style`              | StyleProp\<ViewStyle\> | [{}]           | Estilos adicionais para o wrapper do ícone                                                                       |
| `accessibility`      | string                 | -              | Rótulo de acessibilidade                                                                                         |
| `accessibilityLabel` | string                 | -              | Rótulo de acessibilidade alternativo                                                                             |
| `testID`             | string                 | -              | ID para testes                                                                                                   |
| `id`                 | string                 | -              | ID do componente                                                                                                 |

## Exemplos

### Ícones em uma Barra de Navegação

```tsx
import React from 'react';
import { View } from 'react-native';
import { Icon } from '@platformbuilders/fluid-react-native';

const NavBar = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        justifyContent: 'space-around',
        padding: 16,
        backgroundColor: '#FFFFFF',
      }}
    >
      <Icon
        name="home"
        size={24}
        color="#333333"
        onPress={() => console.log('Home')}
      />
      <Icon
        name="search"
        size={24}
        color="#333333"
        onPress={() => console.log('Search')}
      />
      <Icon
        name="bell"
        size={24}
        color="#333333"
        onPress={() => console.log('Notifications')}
      />
      <Icon
        name="user"
        size={24}
        color="#333333"
        onPress={() => console.log('Profile')}
      />
    </View>
  );
};
```

### Botão com Ícone

```tsx
import React from 'react';
import { Text, TouchableOpacity } from 'react-native';
import { Icon } from '@platformbuilders/fluid-react-native';

const IconButton = ({ onPress }) => {
  return (
    <TouchableOpacity
      style={{
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: '#4CAF50',
        paddingHorizontal: 16,
        paddingVertical: 8,
        borderRadius: 4,
      }}
      onPress={onPress}
    >
      <Icon
        name="check"
        size={18}
        color="white"
        style={{ marginRight: 8 }}
        touchable={false}
      />
      <Text style={{ color: 'white' }}>Confirmar</Text>
    </TouchableOpacity>
  );
};
```

### Ícones com Diferentes Estilos e Cores

```tsx
import React from 'react';
import { View } from 'react-native';
import { Icon, Typography } from '@platformbuilders/fluid-react-native';

const IconShowcase = () => {
  return (
    <View style={{ padding: 16 }}>
      <Typography variant="lg" style={{ marginBottom: 16 }}>
        Redes Sociais
      </Typography>

      <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 16 }}>
        <Icon name="facebook" type="fa-brands" size={30} color="#1877F2" />
        <Icon name="twitter" type="fa-brands" size={30} color="#1DA1F2" />
        <Icon name="instagram" type="fa-brands" size={30} color="#E4405F" />
        <Icon name="linkedin" type="fa-brands" size={30} color="#0A66C2" />
        <Icon name="youtube" type="fa-brands" size={30} color="#FF0000" />
        <Icon name="whatsapp" type="fa-brands" size={30} color="#25D366" />
      </View>

      <Typography variant="lg" style={{ marginTop: 24, marginBottom: 16 }}>
        Interface
      </Typography>

      <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 16 }}>
        <Icon name="home" size={24} color="#333333" />
        <Icon name="search" size={24} color="#333333" />
        <Icon name="heart" size={24} color="#E91E63" />
        <Icon name="shopping-cart" size={24} color="#333333" />
        <Icon name="user" size={24} color="#333333" />
        <Icon name="cog" size={24} color="#333333" />
      </View>

      <Typography variant="lg" style={{ marginTop: 24, marginBottom: 16 }}>
        Status e Ações
      </Typography>

      <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 16 }}>
        <Icon name="check-circle" size={24} color="#4CAF50" />
        <Icon name="exclamation-triangle" size={24} color="#FFC107" />
        <Icon name="times-circle" size={24} color="#F44336" />
        <Icon name="info-circle" size={24} color="#2196F3" />
        <Icon name="question-circle" size={24} color="#9E9E9E" />
        <Icon name="lock" size={24} color="#333333" />
      </View>
    </View>
  );
};
```

### Ícones com Fundo e Borda

```tsx
import React from 'react';
import { View } from 'react-native';
import { Icon } from '@platformbuilders/fluid-react-native';

const IconsWithBackground = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        justifyContent: 'space-around',
        padding: 16,
      }}
    >
      <Icon
        name="plus"
        size={20}
        color="#4CAF50"
        backgroundColor="#E8F5E9"
        style={{ padding: 10, borderRadius: 25 }}
      />

      <Icon
        name="edit"
        size={20}
        color="#2196F3"
        backgroundColor="#E3F2FD"
        style={{ padding: 10, borderRadius: 25 }}
      />

      <Icon
        name="trash"
        size={20}
        color="#F44336"
        backgroundColor="#FFEBEE"
        style={{ padding: 10, borderRadius: 25 }}
      />

      <Icon
        name="bell"
        size={20}
        color="#FFC107"
        backgroundColor="#FFF8E1"
        style={{ padding: 10, borderRadius: 25 }}
      />
    </View>
  );
};
```

## Recursos Adicionais

Todos os ícones disponíveis podem ser encontrados na documentação oficial das bibliotecas de ícones:

- [FontAwesome](https://fontawesome.com/icons)
- [Material Icons](https://materialdesignicons.com/)
- [React Native Vector Icons](https://oblador.github.io/react-native-vector-icons/)
